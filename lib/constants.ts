/** Shared UI constants. */

/** Number of rows shown in each dashboard highlight card (Trending, Gainers, Losers). */
export const HIGHLIGHT_COUNT = 5;

/** Number of headlines shown in the news strip (even, for a 2-column layout). */
export const NEWS_COUNT = 6;
